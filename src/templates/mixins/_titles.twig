{% macro title(text, parentSelector, size, color, tag, split) %}
    <{% if tag %}{{ tag }}{% else %}h2{% endif %} class="{% if parentSelector %} {{ parentSelector }}__title {% endif %}
            title title--{{ size }} {% if color %} title--{{ color }} {% endif %}" {% if split %} data-splitting {% endif %}>
        {{ text | safe }}
    </{% if tag %}{{ tag }}{% else %}h2{% endif %}>
{% endmacro %}

{#usage#}
{#{{ titles.title("Try Restream For Free", "hero", "xl") }}#}
